extends /layout/layout

block htmlhead
    title skeleton-project
    meta(name='description', content='')

block htmlbody
    h1 #{data.title}
    h1 #{data.subtitle}
    +mymodule
      //- Example with default arguments
      +mymodule-submodule
      //- Example with arguments
      +mymodule-submodule("A transcluded title")
