mixin button (data, options)
  - options = setOptionsDefaults(options, "button")
  - var classes = getOptionsModifier(options, "button")

  a(class=classes, disabled=options['disabled']) #{data.label}
