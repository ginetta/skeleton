extends /layout/layout

//- For now we have to include this here, so that we have access
//- to the components exported mixin
include /modules/mymodule/mymodule
include /modules/mymodule/mymodule-submodule/mymodule-submodule

block htmlhead
    meta(name='format-detection', content='telephone=no')

block htmlbody
    include /modules/site/site-header/site-header
    h1 #{translation.hello}
    +mymodule
      //- Example with arguments
      +mymodule-submodule("Super Card")
      //- Example with default arguments
      +mymodule-submodule()
    +mymodule-submodule()
